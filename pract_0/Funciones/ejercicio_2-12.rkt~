;; The first three lines of this file were inserted by DrRacket. They record metadata
;; about the language level of this file in a form that our tools can easily process.
#reader(lib "htdp-beginner-reader.ss" "lang")((modname ejercicio_2) (read-case-sensitive #t) (teachpacks ()) (htdp-settings #(#t constructor repeating-decimal #f #t none #f () #f)))
;2
(define (dist_origen x y)
        (sqrt (+ (expt x 2) (expt y 2))))
;3
(define (dist_puntos x1 y1 x2 y2)
        (sqrt (+ (expt (- x1 x2) 2) (expt (- y1 y2) 2))))

;4
(define (vol-cubo arista)
        (expt arista 3))

;5
(define (area-cubo arista)
        (* (expt arista 2) 6))

;6
(define (metro-pie metros)
        (* metros 3.2808))

;7
(define (cel-far t_en_celsius)
        (+ (* t_en_celsius (/ 9 5)) 32))
;8
(define (posible? a b c)
        (and (and (> (+ a b) c)
                  (> (+ b c) a))
             (> (+ a c ) b)))
;9 COMPLETAR


;10
(define (suma-long a b)
        (+ (string-length a) (string-length b)))

;11 COMPLETAR
(define (comienzaA? a)
        (= () "A"))

;12 COMPLETAR
(define (poner- a)
        ())